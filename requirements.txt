# DMNet — Diabetes Prediction System
# Install: pip install -r requirements.txt
#
# IMPORTANT — Install ORDER matters on Windows:
#   pip install numpy==1.26.4 protobuf==3.20.3
#   pip install tensorflow==2.13.0
#   pip install -r requirements.txt
#
# Or just run:  python fix_environment.py

# ── CRITICAL pins ──────────────────────────────────────────────
# NumPy 2.x breaks TensorFlow 2.13 (_ARRAY_API not found error)
numpy==1.24.3
# protobuf 4.x breaks TF 2.13 (runtime_version import error)
protobuf==3.20.3

# ── Deep Learning ──────────────────────────────────────────────
tensorflow==2.13.0

# ── ML / Data ──────────────────────────────────────────────────
scikit-learn>=1.3.0,<1.6.0
xgboost>=1.7.0,<2.1.0
pandas>=2.0.0,<2.3.0
joblib>=1.3.0

# ── Explainability ─────────────────────────────────────────────
shap>=0.44.0
lime>=0.2.0.1

# ── Visualization ──────────────────────────────────────────────
matplotlib>=3.7.0,<3.10.0
seaborn>=0.12.0

# ── Frontend ───────────────────────────────────────────────────
streamlit>=1.32.0

# ── Backend ────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# ── Miscellaneous ──────────────────────────────────────────────
setuptools
wheel

# This is my requirements.txt